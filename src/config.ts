// Swappable site configuration. Update these as the real details firm up.
// User-facing copy lives in i18n; this file holds non-translatable data only.

/** Wedding date/time (local Israel time). Used by the countdown. */
export const WEDDING_DATE = new Date('2026-09-18T18:30:00+03:00');

/** Google Maps link for the venue. Swap the query for the real address. */
export const MAP_URL =
  'https://www.google.com/maps/search/?api=1&query=Tel+Aviv';

/** Gift methods — swap for real details (or remove the section in Gift.tsx). */
export const PAYBOX_URL = 'https://payboxapp.page.link/';
